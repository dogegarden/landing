import React from "react";
import { motion } from "framer-motion";

export default function Animation(props) {
  return (
    <section>
      {/* <motion.div
        style={{ display: "block", backgroundColor: "red", height: "100px", width: "100px"}}
        animate={{ rotate: 360 }}
    transition={{ duration: 2 }}
      >
        test
      </motion.div>
      <motion.circle style={{ display: "block", backgroundColor: "red" }} cx={500} animate={{ cx: [null, 100] }} /> */}
    </section>
  );
}
